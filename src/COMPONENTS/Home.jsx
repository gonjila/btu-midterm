import styled from 'styled-components';
import Navigation from './Navigation';

const Home = () => {
    return (
        <Container>
            <Navigation />
            haloo
        </Container>
    );
};

export default Home;

const Container = styled.div`
    text-align: center;
`;
